resource:
  id: sample
  name: Test resource
workflows:
  main:
    jobs:
      check:
        sandbox-image: default
        limits:
          memory: 64MiB
          step-timeout: 1s
        steps:
        - run: 'true'
          expected:
            stdout:
              match: exact
              path: missing.txt
