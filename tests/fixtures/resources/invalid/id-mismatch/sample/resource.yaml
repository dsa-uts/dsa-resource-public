resource:
  version: 1
  id: wrong
  name: Test resource
workflows:
  main:
    jobs:
      check:
        sandbox-image: default
        limits:
          memory: 64MiB
          step-timeout: 1s
        steps:
        - run: 'true'
