resource:
  version: 1
  id: sample
  name: Test resource
workflows:
  main:
    jobs:
      check:
        sandbox-image: default
        limits:
          memory: 64MiB
          step-timeout: '2s

            '
        steps:
        - run: 'true'
