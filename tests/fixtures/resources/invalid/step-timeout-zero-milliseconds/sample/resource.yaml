resource:
  id: sample
  name: Test resource
workflows:
  main:
    jobs:
      check:
        sandbox-image: default
        limits:
          memory: 64MiB
          step-timeout: 0ms
        steps:
        - run: 'true'
