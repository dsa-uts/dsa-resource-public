resource:
  id: sample
  name: Test resource
workflows:
  main:
    jobs:
      check:
        sandbox-image: default
        limits:
          memory: 64MiB
          step-timeout: 0s
        steps:
        - run: 'true'
