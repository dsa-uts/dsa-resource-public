resource:
  version: 1
  id: sample
  name: Test resource
workflows:
  main:
    jobs:
      first:
        sandbox-image: default
        limits:
          memory: 64MiB
          step-timeout: 1s
        steps:
        - run: 'true'
        depends:
        - second
      second:
        sandbox-image: default
        limits:
          memory: 64MiB
          step-timeout: 1s
        steps:
        - run: 'true'
        depends:
        - first
